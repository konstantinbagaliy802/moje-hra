#include <iostream>
using namespace std;

int main() {
   
    int hp = 0, maxHp = 0;
    int utok = 0;
    int mana = 0, maxMana = 0;
    int zlato = 20;
    int level = 1;
    int zkusenosti = 0;
	int schopnost1 = 0;
	int schopnost2 = 0;

    int volba;
    bool potvrzeno = false;

    cout << "=== CESTA ZA SILENTCHANTEM ===" << endl;

    while (potvrzeno == false) {
        cout << "\nVyber si svoji postavu:" << endl;
        cout << "1) Rytir " << endl;
        cout << "2) Mag " << endl;
		cout << "3) Lukostrelec " << endl;
        cout << "Vase volba: ";
        cin >> volba;

        if (volba == 1) {
            hp = 25; maxHp = 25;
            utok = 7;
            mana = 5; maxMana = 5;
        }
        else if (volba == 2) {
            hp = 8; maxHp = 8;
            utok = 6;
            mana = 20; maxMana = 20;
		}
        else if (volba == 3) {
            hp = 12; maxHp = 12;
            utok = 8;
            mana = 10; maxMana = 10;
        }

        if (volba == 1 || volba == 2 || volba == 3) {
            cout << "\n--- STATISTIKY POSTAVY A SCHOPNOSTI ---" << endl;
           
            if (volba == 1) {
                cout << "Class: Rytir" << endl;
			    cout << "Schopnost 1: Silny utok (Odebere nepritelovi 11 Hp)" << endl;
                cout << "Schopnost 2: Stit (blokuje utok)" << endl;
			}
            if (volba == 2) {
                cout << "Class: Mag" << endl;
                cout << "Schopnost 1: Fireball (Odebere nepritelovi 12 Hp, stoji to 7 many)" << endl;
                cout << "Schopnost 2: Leceni (Prida sobe 5 Hp, stoji to 5 many)" << endl;
            }
            if (volba == 3) {
                cout << "Class: Lukostrelec" << endl;
                cout << "Schopnost 1: Jedovaty sip (Odebere nepritelovi 10 Hp + jed)" << endl;
                cout << "Schopnost 2: Prubojny sip (Odebere nepritelovi 7 Hp, stoji to 4 many)" << endl;
            }
                cout << "Zivoty: " << hp << "/" << maxHp << endl;
                cout << "Utok: " << utok << endl;
                cout << "Mana: " << mana << "/" << maxMana << endl;

                cout << "\nChces tuto postavu? (1 - Ano / 0 - Vybrat jinou): ";
            int potvrzeni;
            cin >> potvrzeni;

            if (potvrzeni == 1) {
                potvrzeno = true;
            }
        }
        else {
            cout << "Neplatna volba, zkus to znovu!" << endl;
        }
    }

    cout << "\nPostava potvrzena." << endl;

    int obchod_volba;
    bool odejit_z_vesnice = false;

    cout << "\nDorazili jste do vesnice ---. Mas u sebe " << zlato << " zlata. " << endl;

    while (odejit_z_vesnice == false) {
        cout << "\n--- VESNICE A OBCHODY --- " << endl;
        cout << "Kam chcete jit? " << endl;
        cout << "1) Hospoda U --- (Muzes se najist a pricte ti to +5 k max HP - za jidlo musis zaplatit 10 zlatych). " << endl;
        cout << "2) Cirkev (Kdyz poprosis aby ti rekli modlitbu na zdravi tak se ti pricte +5 k max mane - stoji to 10 zlatych) " << endl;
        cout << "3) Do pole (Potkas tam moudreho rytire ktery ti muze natrenovat a pricte ti to +2 k utoku - poprosi abys mu dal 5 zlatych) " << endl;
        cout << "4) Odejit z vesnice " << endl;
        cout << "Tve zlato: " << zlato << " | Tvuj utok: " << utok << " | Tvuj max HP: " << maxHp << " | Tvoje max mana: " << maxMana << endl;
        cout << "Tvoje volba: " << endl;
        cin >> obchod_volba;

            if (obchod_volba == 1) {
        if (zlato >= 10) {
            maxHp = maxHp + 5;
            hp = maxHp;
            zlato = zlato - 10;
            cout << "--> Jses syty a stastny " << endl;
        }
        else {
            cout << "--> Mas malo penez " << endl;
        }
    }
    else if (obchod_volba == 2) {
        if (zlato >= 10) {
            maxMana = maxMana + 5;
            mana = maxMana;
            zlato = zlato - 10;
            cout << "--> Citis se lepe a mas dobrou naladu " << endl;
        }
        else {
            cout << "--> Mas malo penez " << endl;
        }
    }
    else if (obchod_volba == 3) {
        if (zlato >= 5) {
            utok = utok + 2;
            zlato = zlato - 5;
            cout << "--> Mas pocit ze jsi silnejsi nez byl " << endl;
        }
        else {
            cout << "--> Mas malo penez " << endl;
        }
    }
    else if (obchod_volba == 4) {
        odejit_z_vesnice = true;
        cout << "Opoustis vesnici ---..." << endl;
    }
    else {
        cout << "Neplatna volba!" << endl;
    }
}

    cout << "\n==========================================" << endl;
    cout << "Vstoupil jsi do temneho lesa " << endl;
    cout << "Z krovi na tebe vyskocil cerny vlk!" << endl;

    int vlk_hp = 30;
    int vlk_utok = 5;
    int souboj_volba;

    while (hp > 0 && vlk_hp > 0) {
        cout << "Tvoje HP: " << hp << "/" << maxHp << " | Mana: " << mana << "/" << maxMana << " | HP Vlka: " << vlk_hp << endl;
        cout << "Co udelas?" << endl;
        cout << "1) Normalni utok (Zpusobi " << utok << " poskozeni)" << endl;

        if (volba == 1) {
            cout << "2) Silny utok (Zpusobi 11 poskozeni, stoji to 0 many)" << endl;
            cout << "3) Stit (Zablokujes utok vlka, stoji to 0 many)" << endl;
        }
        else if (volba == 2) {
            cout << "2) Fireball (Zpusobi 12 poskozeni, stoji to 7 many)" << endl;
            cout << "3) Leceni (Prida 5 HP, stoji to 5 many)" << endl;
        }
        else if (volba == 3) {
            cout << "2) Jedovaty sip (Zpusobi 10 poskozeni, stoji to 0 many)" << endl;
            cout << "3) Prubojny sip (Zpusobi 7 poskozeni, stoji to 4 many)" << endl;
        }

        cout << "Vase volba: ";
        cin >> souboj_volba;

        bool hrac_blokuje = false;

        if (souboj_volba == 1) {
            vlk_hp = vlk_hp - utok;
            cout << "--> Sekl jsi vlka a zpusobil jsi mu " << utok << " poskozeni " << endl;
        }

        else if (souboj_volba == 2) {
            if (volba == 1) {
                vlk_hp = vlk_hp - 11;
                cout << "--> Pouzil jsi Silny utok a zpusobil vlkovi 11 poskozeni " << endl;
            }
            else if (volba == 2) {
                if (mana >= 7) {
                    vlk_hp = vlk_hp - 12;
                    mana = mana - 7;
                    cout << "--> Hodil jsi fireball! Vlk dostal za 12 poskozeni " << endl;
                }
                else {
                    cout << "--> Mas malo many! Utok selhal a nic jsi neudelal " << endl;
                }
            }
            else if (volba == 3) {
                vlk_hp = vlk_hp - 10;
                cout << "--> Vystrelil jsi jedovaty sip a zpusobil vlkovi 10 poskozeni  " << endl;
            }
        }

        else if (souboj_volba == 3) {
            if (volba == 1) {
                hrac_blokuje = true;
                cout << "--> Pozvedl jsi stit a jsi pripraven branit se " << endl;
            }
            else if (volba == 2) {
                if (mana >= 5) {
                    hp = hp + 5;
                    if (hp > maxHp) hp = maxHp;
                    mana = mana - 5;
                    cout << "--> Seslal jsi Leceni a vylecil sis 5 HP " << endl;
                }
                else {
                    cout << "--> Mas malo many! Kouzlo selhalo a nic jsi neudelal " << endl;
                }
            }
            else if (volba == 3) {
                if (mana >= 4) {
                    vlk_hp = vlk_hp - 7;
                    mana = mana - 4;
                    cout << "--> Prubojny sip proletel vlkem za 7 poskozeni " << endl;
                }
                else {
                    cout << "--> Mas malo many! Vystrel selhal " << endl;
                }
            }
        }
        else {
            cout << "Zmatkoval jsi a nic jsi neudelal " << endl;
        }

        if (vlk_hp > 0) {
            if (hrac_blokuje == true) {
                cout << "--> Vlk skocil, ale tvuj Stit utok kompletne zablokoval " << endl;
            }
            else {
                hp = hp - vlk_utok;
                cout << "--> Vlk utoci! Kousl te a odebral " << vlk_utok << "HP" << endl;
            }
        }
    }

    if (hp > 0) {
        cout << "Porazil jsi cerneho vlka" << endl;
        zlato = zlato + 15;
        zkusenosti = zkusenosti + 20;
        cout << "Ziskavas 15 zlata a 20 zkusenosti" << endl;
        cout << "Tvoje aktualni zlato: " << zlato << endl;
    }
    else {
        cout << "Zemrel jsi " << endl;
    }
    return 0;
}

